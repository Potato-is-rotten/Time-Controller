<Window x:Class="ScreenTimeController.Wpf.Views.ChangePasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ScreenTimeController.Wpf.MarkupExtensions"
        Title="{local:Localize ChangePassword}" 
        Height="400" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F0F0F0"
        FontSize="14"
        WindowStyle="SingleBorderWindow"
        KeyDown="OnKeyDown">
    
    <Window.Resources>
        <Style x:Key="LightButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#C8C8C8"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D8D8D8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="PrimaryLightButtonStyle" TargetType="Button" BasedOn="{StaticResource LightButtonStyle}">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0098FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource LightButtonStyle}">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E74C5C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,20">
            <TextBlock Text="{local:Localize NewPassword}" 
                       FontSize="18" FontWeight="Bold"
                       Width="200" VerticalAlignment="Center"/>
            <PasswordBox x:Name="NewPasswordBox"
                         Width="400" Height="40"
                         FontSize="16"
                         VerticalContentAlignment="Center"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20">
            <TextBlock Text="{local:Localize ConfirmPassword}" 
                       FontSize="18" FontWeight="Bold"
                       Width="200" VerticalAlignment="Center"/>
            <PasswordBox x:Name="ConfirmPasswordBox"
                         Width="400" Height="40"
                         FontSize="16"
                         VerticalContentAlignment="Center"/>
        </StackPanel>
        
        <TextBlock Grid.Row="2" 
                   Text="{Binding StatusMessage}"
                   FontSize="14" FontWeight="Bold"
                   Foreground="#DC3545"
                   Margin="0,10"
                   Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,20">
            <Button Content="{local:Localize OK}"
                    Style="{StaticResource PrimaryLightButtonStyle}"
                    Command="{Binding OKCommand}"
                    Width="130" Height="50" Margin="0,0,20,0"/>
            
            <Button Content="{local:Localize Cancel}"
                    Style="{StaticResource LightButtonStyle}"
                    Command="{Binding CancelCommand}"
                    Width="130" Height="50" Margin="0,0,20,0"/>
            
            <Button Content="{local:Localize RemovePassword}"
                    Style="{StaticResource DangerButtonStyle}"
                    Command="{Binding RemovePasswordCommand}"
                    Width="180" Height="50"/>
        </StackPanel>
    </Grid>
</Window>
