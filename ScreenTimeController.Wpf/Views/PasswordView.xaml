<Window x:Class="ScreenTimeController.Wpf.Views.PasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ScreenTimeController.Wpf.MarkupExtensions"
        Title="{local:Localize EnterPassword}" 
        Height="350" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F0F0F0"
        FontSize="14"
        WindowStyle="SingleBorderWindow"
        KeyDown="OnKeyDown">
    
    <Window.Resources>
        <Style x:Key="LightButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#C8C8C8"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D8D8D8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="PrimaryLightButtonStyle" TargetType="Button" BasedOn="{StaticResource LightButtonStyle}">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0098FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,20">
            <TextBlock Text="{local:Localize Password}" 
                       FontSize="18" FontWeight="Bold"
                       Width="150" VerticalAlignment="Center"/>
            <PasswordBox x:Name="PasswordBox"
                         Width="340" Height="40"
                         FontSize="16"
                         VerticalContentAlignment="Center"
                         IsEnabled="{Binding CanEnterPassword}"/>
        </StackPanel>
        
        <TextBlock Grid.Row="1" 
                   Text="{Binding LockStatus}"
                   FontSize="14" FontWeight="Bold"
                   Foreground="Red"
                   Margin="0,10"
                   Visibility="{Binding LockStatus, Converter={StaticResource StringToVisibilityConverter}}"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,20">
            <Button Content="{local:Localize OK}"
                    Style="{StaticResource PrimaryLightButtonStyle}"
                    Command="{Binding OKCommand}"
                    Width="120" Height="50" Margin="0,0,30,0"/>
            
            <Button Content="{local:Localize Cancel}"
                    Style="{StaticResource LightButtonStyle}"
                    Command="{Binding CancelCommand}"
                    Width="120" Height="50"/>
        </StackPanel>
    </Grid>
</Window>
