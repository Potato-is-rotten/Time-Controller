<Window x:Class="ScreenTimeController.Wpf.Views.InstallWizardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ScreenTimeController.Wpf.MarkupExtensions"
        Title="{local:Localize InstallationWizard}" 
        Height="420" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        FontSize="14"
        WindowStyle="SingleBorderWindow">
    
    <Window.Resources>
        <Style x:Key="DarkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#505050"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#606060"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#404040"/>
                    <Setter Property="Foreground" Value="#808080"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DarkButtonStyle}">
            <Setter Property="Background" Value="#007ACC"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0098FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="{local:Localize InstallationWizard}"
                   FontSize="20" FontWeight="Bold" 
                   Foreground="White"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>
        
        <TextBlock Grid.Row="1" 
                   Text="{local:Localize InstallationWizardDescription}"
                   FontSize="12" 
                   Foreground="#C8C8C8"
                   TextWrapping="Wrap"
                   Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="2" Margin="10,0">
            <CheckBox Content="{local:Localize ProtectionService}"
                      Style="{StaticResource CheckBoxStyle}"
                      IsChecked="{Binding InstallProtectionService}"
                      IsEnabled="{Binding CanInteract}"/>
            
            <CheckBox Content="{local:Localize WatchdogProcess}"
                      Style="{StaticResource CheckBoxStyle}"
                      IsChecked="{Binding InstallWatchdog}"
                      IsEnabled="{Binding CanInteract}"/>
            
            <CheckBox Content="{local:Localize ScheduledTask}"
                      Style="{StaticResource CheckBoxStyle}"
                      IsChecked="{Binding InstallScheduledTask}"
                      IsEnabled="{Binding CanInteract}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Margin="0,20">
            <ProgressBar Height="4" 
                         Value="{Binding Progress}"
                         Maximum="100"
                         Visibility="{Binding IsInstalling, Converter={StaticResource BoolToVisibilityConverter}}"/>
            
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="#64FF64"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"
                       Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="{local:Localize Install}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding InstallCommand}"
                    Width="120" Margin="0,0,20,0"/>
            
            <Button Content="{local:Localize Skip}"
                    Style="{StaticResource DarkButtonStyle}"
                    Command="{Binding SkipCommand}"
                    Width="120"/>
        </StackPanel>
    </Grid>
</Window>
